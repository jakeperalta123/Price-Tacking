services:
  db:
    image: postgres:15
    container_name: postgres_price_tracker
    restart: always
    environment:
      POSTGRES_DB: price_track
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: iamrich56987
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: