services:
  db:
    image: mysql:8.0
    container_name: mysql_price_tracker
    restart: always
    environment:
      MYSQL_DATABASE: price_tracker
      MYSQL_ROOT_PASSWORD: test123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: